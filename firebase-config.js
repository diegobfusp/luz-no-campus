// Cole aqui a configuração do seu projeto Firebase
// (Console do Firebase > Configurações do projeto > Seus apps > App da Web > Configuração).
// Enquanto os valores abaixo estiverem como "COLE_AQUI", o app roda em MODO DEMONSTRAÇÃO:
// as marcações ficam só no aparelho e ninguém mais as vê.
//
// Estas chaves NÃO são secretas: podem ficar num repositório público.
// Quem protege os dados são as regras do Firestore (arquivo firestore.rules).

export const firebaseConfig = {
  apiKey: "COLE_AQUI",
  authDomain: "COLE_AQUI",
  projectId: "COLE_AQUI",
  storageBucket: "COLE_AQUI",
  messagingSenderId: "COLE_AQUI",
  appId: "COLE_AQUI"
};
